{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/http://localhost:5000/(.*)",
            "dest": "server.js"
        }
    ]
}